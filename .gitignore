# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# `tsc -b` incremental state. Machine-local and rewritten by every build; one
# was committed once and then showed up as a diff on every checkout.
*.tsbuildinfo

# Written by `npm run test:coverage`. CI reads the lcov report from the run
# itself, so there is never a reason to commit one.
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
