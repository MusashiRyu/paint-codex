# Architecture

## Component Table

| Component | Path | Responsibility | Notes |
| --- | --- | --- | --- |
| App shell | `src/app/App.tsx` | Card/viewport layout, FAB, overlay state (search sheet, new list sheet), export trigger, list rename wiring. | Lists-first redesign; previous browse/search/filter wiring removed. Registers both sheets with `useBackDismiss`. Passes `onExportList` only when the `markdownExport` flag is on. |
| Feature flags | `src/app/config.ts` | Single `appConfig.featureFlags` object of compile-time toggles. | `markdownExport: false` — the export download action is off for now. |
| App styles | `src/app/App.module.css` | Viewport dark-background, centered card, header, divider, FAB styling. | Card is full-screen on mobile; centered 390px card on desktop ≥500px. |
| Store | `src/app/providers/store.ts` | Persisted Zustand store; manages lists, selected list, and paint membership. | A list holds `paintIds: string[]`, never Paint objects — see the conventions below. `PaintList` also carries `icon` and `color`. Persist is at version 2; `migrate` is cumulative, so a version 0 store picks up both steps in one pass. |
| Paint repository | `src/domain/paintRepository.ts` | Bundled catalogue access: `getPaints`, `getPaintIndex`, `resolvePaints`. | `resolvePaints` turns stored ids into catalogue entries and drops ids that no longer exist, since a rescrape can retire a paint. |
| List icon | `src/shared/ui/ListIconSvg.tsx` | Renders one of 8 SVG emblems (skull/star/shield/wing/crown/flame/moon/drop) for list tabs. | Pure presentational component. |
| Lists panel | `src/features/lists/ListsPanel.tsx` / `ListsPanel.module.css` | Tab bar (list selection + "+ New") and scrollable content (paints grouped by category, empty state, list header actions), plus inline list rename. | Pencil action renames the list: swaps the header name for an input. Enter/blur commits, Escape or back cancels, switching tabs cancels. |
| Paint item | `src/features/lists/PaintItem.tsx` / `PaintItem.module.css` | Single paint row in the list view (swatch, name, brand/type, hex, remove button). | The "×" remove button is always visible; the old `editMode` prop and left "–" button were removed as redundant. |
| Back dismiss hook | `src/shared/hooks/useBackDismiss.ts` | LIFO stack of dismissable layers; routes the Android back gesture to the topmost open layer and only exits the app when none remain. | Requires the `@capacitor/app` plugin — Capacitor core has no back handling, so back would otherwise finish the activity. No-op on web. |
| Escape dismiss hook | `src/shared/hooks/useDismissOnEscape.ts` | Closes the layer on Escape; desktop counterpart to `useBackDismiss`. | Only mounted layers listen and the handler stops propagation, so nested layers close innermost-first. |
| Search sheet | `src/features/search/SearchSheet.tsx` / `SearchSheet.module.css` | Bottom-sheet search overlay: text input, brand filter chips, fuzzy results with equivalents grid, ΔE pills, IN LIST badge, + add button. | Uses `search.ts` Fuse.js logic; opened by FAB or "+ ADD FIRST PAINT". Brand chips derive from the catalogue; equivalents go through `getTopMatches` so they render closest-first and capped at `CLOSE_DELTA_MAX`. `role="dialog"` + `aria-modal`. |
| New list sheet | `src/features/lists/NewListSheet.tsx` / `NewListSheet.module.css` | Bottom-sheet for creating a list: name input, 8-icon emblem picker, 5-color banner picker, CREATE LIST button. | CREATE LIST is disabled until name is non-empty. `role="dialog"` + `aria-modal`. |
| Export logic | `src/features/export/markdownExport.ts` | Generates markdown for a paint list with shop links. | Takes an `ExportableList` (name + resolved paints), not a `PaintList`, so it has no dependency on the store. Only wired up when `appConfig.featureFlags.markdownExport` is true. |
| Fonts | `src/shared/styles/fonts.css`, `public/fonts/` | Self-hosted Cinzel + EB Garamond (SIL OFL 1.1), latin and latin-ext subsets. | Not a CDN: the packaged app has no network guarantee and would otherwise fall back to a system serif offline. Regenerate with `tools/fonts/vendor-fonts.mjs`. |
| CI | `.github/workflows/ci.yml` | Lint, typecheck, test and build on push and pull request. | Build step runs `vite build` directly; the typecheck step already covers `tsc -b`. |
| Search logic | `src/features/search/search.ts` | Fuse.js fuzzy search over the paint catalog. | Unchanged; used by SearchSheet. |
| Paint queries | `src/domain/paintQueries.ts` | Pure queries over the catalogue: `getUniqueBrands`, `getTopMatches`, `filterPaintsByColor`. | Moved out of the deleted `features/browse` so views share them without importing across features. `filterPaintsByColor` is currently unused. |
| Color utilities | `src/shared/lib/color.ts` | `hexToHSL`, plus the single source of truth for ΔE classification: `CLOSE_DELTA_MAX` (7), `VERY_CLOSE_DELTA_MAX` (3), `getDeltaQuality`, `getDeltaStyle`, `getDeltaColorClass`, `getDeltaLabel`. | Every view that renders a delta must classify through here — SearchSheet previously used its own 2/4 scale. |
| Global styles | `src/shared/styles/global.css` | CSS variables (gold palette, text, state colors), base reset, font/scroll defaults. | Complete restyle; old shell classes removed; Cinzel + EB Garamond fonts. |
| Background images | `public/imagery/bg-page.png`, `public/imagery/bg-card.png` | Dark texture (page bg) and dark swirling abstract art (card bg). | Sourced from Claude Design mockup. |
| Tests | `src/test/` | Unit tests for store, search, export, lists panel, and search sheet. | 63 tests pass. `ListsPanel.test.tsx` covers both flag states of the export action by supplying or omitting `onExportList`. |

## Conventions

- **TypeScript is strict everywhere.** Every project (`app`, `test`, `node`, `tools`) sets `strict: true`. Test files are typechecked by `tsconfig.test.json`; they were excluded from every project until retro 005.
- **Delta classification goes through `shared/lib/color.ts`.** Never inline a threshold in a component.
- **Never render `paint.matches` directly** — the snapshot stores them unordered. Go through `getTopMatches`.
- **Overlays register with both dismiss hooks**, `useBackDismiss` (Android) and `useDismissOnEscape` (desktop), and carry `role="dialog"` + `aria-modal`.
- **Lists persist paint ids, never Paint objects.** Resolve with `resolvePaints` at read time. Storing copies means a catalogue refresh never reaches saved lists. Any change to the persisted shape needs a `version` bump and a cumulative `migrate` branch.
- **No external runtime dependencies.** Fonts are self-hosted and the catalogue is bundled; the app makes no network request of its own. Keep it that way.

## Native

| Concern | Where | Notes |
| --- | --- | --- |
| Android back gesture | `@capacitor/app` + `src/shared/hooks/useBackDismiss.ts` | Capacitor 8 core registers no `onBackPressed`/`OnBackInvoked` handler, so the gesture reaches Android's default and finishes the activity. The plugin surfaces a `backButton` event the web layer can intercept. |
| Android toolchain | `android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.14.3 requires **JDK 21**. Android Studio's bundled JBR is Java 25 and fails with `Unsupported class file major version 69` once cached build scripts are invalidated. |
