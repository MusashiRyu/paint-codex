# Architecture

## Component Table

| Component | Path | Responsibility | Notes |
| --- | --- | --- | --- |
| App shell | `src/app/App.tsx` | Card/viewport layout, FAB, header About action, overlay state (search sheet, new list sheet, about sheet), export trigger, list rename wiring. | Lists-first redesign; previous browse/search/filter wiring removed. Registers all three sheets with `useBackDismiss`. Passes `onExportList` only when the `markdownExport` flag is on. |
| Feature flags and links | `src/app/config.ts` | `appConfig.featureFlags` (compile-time toggles), `appConfig.links` (every URL the app can send someone to), and `APP_VERSION`. | `markdownExport: false` — the export download action is off for now. `supportLink: true` — it toggles the About sheet's support section. `APP_VERSION` is the web-only fallback for the version display and is pinned to `android/app/build.gradle` by `src/test/appVersion.test.ts`, which pins the two iOS build settings to it as well — `versionName` and `versionCode` are the only version values anyone edits. |
| App styles | `src/app/App.module.css` | Viewport dark-background, centered card, header, divider, FAB styling. | Card is full-screen on mobile; centered 390px card on desktop ≥500px. |
| Store | `src/app/providers/store.ts` | Persisted Zustand store; manages lists, selected list, and paint membership. | A list holds `paintIds: string[]`, never Paint objects — see the conventions below. `PaintList` also carries `icon` and `color`. Persist is at version 4; `migrate` is cumulative, so a version 0 store picks up every step in one pass. v3 and v4 both rewrite paint ids through `src/data/paintIdMigration.json` — the id scheme changed twice, and the map is built to be safe to apply more than once. |
| Paint repository | `src/domain/paintRepository.ts` | Holds the live catalogue and publishes replacements: `getPaints`, `subscribeToPaints`, `setPaints`, `getPaintIndex`, `resolvePaints`. | Seeded synchronously at import from the runtime cache, falling back to the bundled snapshot, so there is never a paintless first render. `resolvePaints` turns stored ids into catalogue entries and drops ids that no longer exist, since a refresh can retire a paint. The id index is memoised against the array it was built from, so replacing the catalogue invalidates it automatically. |
| Catalogue parser | `src/domain/paintCatalogSource.ts` | `PAINT_CATALOG_SOURCES` plus `parsePaintCatalog(documents)`: reads one markdown table per brand, merges rows that are one colour under several range names, assigns range-qualified ids, and computes every paint's cross-brand equivalents in CIELAB. | The one parser, shared by the build-time scraper and the runtime refresh — see the conventions below. Pure string and arithmetic work, no DOM, so Node can run it too. Columns are located by heading rather than position, because Citadel's table has no `Code` column and the other two do. Validates as it goes: colours normalise to `#RRGGBB` or the entry is dropped, and a row whose range upstream left null is dropped. |
| Catalogue refresh | `src/domain/paintCatalogSync.ts` | `refreshPaintCatalog()` — fetch all three brand documents, validate, adopt. Returns `updated` / `unchanged` / `rejected` / `failed`; never rejects. | Fired once per launch from `main.tsx`, at idle, unawaited. One `AbortController` covers the whole round, so the deadline is the round's rather than resetting per brand, and a partial fetch fails outright. Skips parsing when the combined hash is unchanged, and skips the render when changed documents parse to the same paints. `findShortfall` rejects a parse under half the bundled count in total *or* in any single brand. |
| Catalogue cache | `src/domain/paintCatalogCache.ts` | localStorage persistence of a refreshed catalogue under `paco-paint-catalog`. | Stores a signature of the snapshot the writing build shipped; a mismatch drops the cache, so releasing a corrected snapshot actually reaches users who cached a bad one. Anything unreadable is discarded, never repaired. Storage failures cost persistence, not the session. |
| Catalogue hook | `src/app/providers/usePaintCatalog.ts` | `useSyncExternalStore` binding from the repository to React. | The only place React meets the catalogue, which is what keeps `domain/` React-free. |
| String hash | `src/shared/lib/hash.ts` | `hashString` — FNV-1a, 32-bit. | Change detection only. Lets us ask "same documents as last time?" without keeping 270 KB around to diff. |
| List icon | `src/shared/ui/ListIconSvg.tsx` | Renders one of 8 SVG emblems (skull/star/shield/wing/crown/flame/moon/drop) for list tabs. | Pure presentational component. |
| Sheet | `src/shared/ui/Sheet.tsx` / `Sheet.module.css` | Bottom sheet: backdrop, rounded lip, header with close action. `size="auto"` sizes to content, `size="tall"` is a fixed-height flex column whose body scrolls. | Owns the dismissal and focus contract so a caller cannot wire half of it: Escape, focus trap, `role="dialog"`, `aria-modal`, `tabIndex={-1}`. `useBackDismiss` stays with the caller because the open flag does. Replaced 43 byte-identical lines duplicated across the two overlays. |
| Pill | `src/shared/ui/Pill.tsx` / `Pill.module.css` | Interactive chip: list tabs, brand filters, the dashed "+ New" slot. | Always a button — every pill selects something. `style` is the escape hatch for a tab tinted with a list's user-chosen colour, which cannot come from a token. |
| Badge | `src/shared/ui/Badge.tsx` / `Badge.module.css` | Static status label: the IN LIST badge and the ΔE delta pill. | Never interactive; anything clickable is a `Pill`. `block` is the full-width centred variant under an equivalent tile. The delta pill's colour arrives via `style` from `shared/lib/color.ts`. |
| IconButton | `src/shared/ui/IconButton.tsx` / `IconButton.module.css` | Round outline-only icon button — header actions and the inline remove. | Requires `label`, which sets both the accessible name and the tooltip, because these buttons never have visible text. `danger` is outline-only on purpose so a delete cannot out-weigh the gold action beside it. |
| GoldButton | `src/shared/ui/GoldButton.tsx` / `GoldButton.module.css` | The filled gold gradient action: the FAB (`lg`) and the add button (`md`). | The app's only solid fill, and deliberately only two instances — a third stops it reading as *the* action. Positioning is the caller's via `className`; the button owns only its look. |
| GhostButton | `src/shared/ui/GhostButton.tsx` / `GhostButton.module.css` | Outlined pill action with a text label — the workhorse button. | `quiet` drops the fill for an offer rather than an urged action. Replaced the ad-hoc `addFirstBtn` / `createBtn` / `browseBtn`; CREATE LIST now uses native `disabled` instead of `pointerEvents: none`. Given `href` it renders an `<a>` instead — a union type, because a control that leaves the app has nothing to disable and no handler to run. The stylesheet states `display`, `text-align` and `text-decoration` so the two renderings are identical rather than nearly so. |
| ExternalLink | `src/shared/ui/ExternalLink.tsx` / `ExternalLink.module.css` | The inline text link out of the app. | The only anchor style there is: Paco has no internal navigation, so every link in it is a handoff to the browser. Colour comes from the global `a` rule; only the underline offset is local. |
| Outbound link contract | `src/shared/lib/externalLink.ts` | `EXTERNAL_LINK_PROPS` — the `target`/`rel` pair every outbound link carries. | `target="_blank"` is what makes Capacitor's WebView hand the URL to the system browser; without it the page loads *inside* the app and replaces the UI. `noreferrer` is what makes the privacy policy's "the destination is not told where you came from" true. One constant so that if a device ever fails to hand off and `@capacitor/browser` is needed, one file changes. |
| Action icon spec | `src/shared/ui/actionIcon.ts` | `ACTION_ICON` — shared geometry for the stroked line glyphs. | Was local to `ListsPanel` until the header About button needed the same 24px grid and stroke weight. An icon that sets its own stroke or fill is a bug. |
| TextField | `src/shared/ui/TextField.tsx` / `TextField.module.css` | Text input: `pill` is the standing sheet input, `inline` is edit-in-place. | Spreads the remaining native input props, because the blur/keydown handling an edit-in-place rename needs belongs to the caller. |
| Swatch | `src/shared/ui/Swatch.tsx` / `Swatch.module.css` | A block of colour with an inset dark ring. | The ring is inset so it sits inside the radius — a swatch must read as the paint, not as a bordered box. `as="span"` for swatches inside a button, which may only hold phrasing content. |
| Lists panel | `src/features/lists/ListsPanel.tsx` / `ListsPanel.module.css` | Tab bar (list selection + "+ New") and scrollable content (paints grouped by category, empty state, list header actions), plus inline list rename. | Pencil action renames the list: swaps the header name for an input. Enter/blur commits, Escape or back cancels, switching tabs cancels. The header is gated on the list existing, not on it having paints, so an empty list can still be renamed and deleted. Delete is always available — deleting the last list is allowed and drops to the "No lists yet" state. |
| Paint item | `src/features/lists/PaintItem.tsx` / `PaintItem.module.css` | Single paint row in the list view (swatch, name, brand/type, hex, remove button). | The "×" remove button is always visible; the old `editMode` prop and left "–" button were removed as redundant. |
| Back dismiss hook | `src/shared/hooks/useBackDismiss.ts` | LIFO stack of dismissable layers; routes the Android back gesture to the topmost open layer and only exits the app when none remain. | Requires the `@capacitor/app` plugin — Capacitor core has no back handling, so back would otherwise finish the activity. No-op on web. |
| Escape dismiss hook | `src/shared/hooks/useDismissOnEscape.ts` | Closes the layer on Escape; desktop counterpart to `useBackDismiss`. | Only mounted layers listen and the handler stops propagation, so nested layers close innermost-first. |
| Focus trap hook | `src/shared/hooks/useFocusTrap.ts` | Holds keyboard focus inside an overlay and returns it to the trigger on close. Returns a ref for the container, which needs `tabIndex={-1}`. | `aria-modal` describes a layer as modal without making it behave that way. The trigger is captured during render, not in an effect — React applies `autoFocus` while committing, so an effect would capture the input instead. Visibility filtering is attribute-based on purpose: `offsetParent` reports nothing for `position: fixed` sheets and nothing at all under jsdom. |
| Search sheet | `src/features/search/SearchSheet.tsx` / `SearchSheet.module.css` | Bottom-sheet search overlay: text input, brand filter chips, fuzzy results with equivalents grid, ΔE pills, IN LIST badge, + add button. | Uses `search.ts` Fuse.js logic; opened by FAB or "+ ADD FIRST PAINT". Brand chips derive from the catalogue; equivalents go through `getTopMatches` so they render closest-first and capped at `CLOSE_DELTA_MAX`. `role="dialog"` + `aria-modal`. Each equivalent tile is a button that jumps to the paint it names: the query and brand filter move to that paint, its row is highlighted and focused, and it is pinned into the results if the fuzzy search misses it — so an equivalent can be added in one more tap. The equivalents grid is `repeat(auto-fill, minmax(104px, 1fr))`, so the tiles widen into the card rather than leaving a gutter on the right that does not match the left padding. |
| New list sheet | `src/features/lists/NewListSheet.tsx` / `NewListSheet.module.css` | Bottom-sheet for creating a list: name input, 8-icon emblem picker, 5-color banner picker, CREATE LIST button. | CREATE LIST is disabled until name is non-empty. `role="dialog"` + `aria-modal`. |
| About sheet | `src/features/about/AboutSheet.tsx` / `AboutSheet.module.css` | The app's only informational surface: what Paco is, credits, privacy, the trademark disclaimer and the version. | Reached from the header About action; there is no other route to it. The credits are the load-bearing part — the paint data and the design are both other people's work, and before this sheet that was acknowledged only in the store listing, which nobody reads after installing. Reads the version from `App.getInfo()` on device and falls back to `APP_VERSION` on web, where there is no native package to ask. |
| Export logic | `src/features/export/markdownExport.ts` | Generates markdown for a paint list with shop links. | Takes an `ExportableList` (name + resolved paints), not a `PaintList`, so it has no dependency on the store. Only wired up when `appConfig.featureFlags.markdownExport` is true. |
| Fonts | `src/shared/styles/fonts.css`, `public/fonts/` | Self-hosted Cinzel + EB Garamond (SIL OFL 1.1), latin and latin-ext subsets. | Not a CDN: the packaged app has no network guarantee and would otherwise fall back to a system serif offline. Regenerate with `tools/fonts/vendor-fonts.mjs`. |
| CI | `.github/workflows/ci.yml` | Lint, typecheck, test and build on push and pull request. | Build step runs `vite build` directly; the typecheck step already covers `tsc -b`. |
| Lint config | `.oxlintrc.json` | oxlint's default `correctness` set, the `react`/`typescript`/`oxc`/`jsx-a11y` plugins, and the two `no-restricted-imports` boundaries. | `jsx-a11y` is on because nothing else checks accessibility here; the four rules switched off carry their reason inline. The wider categories are deliberately not enabled — see the README. The import boundaries are the structural rule the whole `tools/` split rests on. |
| Build config | `vite.config.ts` | The React plugin, the `@` → `src` alias, and `build.chunkSizeWarningLimit`. | The limit is raised to 1600 kB because ~1.1 MB of the bundle is `paints.snapshot.json` and that is deliberate — the default warning asks for a dynamic import, which is precisely what the synchronous seeding in `paintRepository` forbids. Raised rather than switched off, and set just above the current size, so real bloat still trips it. |
| Design system barrel | `src/index.ts` | The design system's declared public surface: every `shared/ui` primitive, the list palette, the ΔE classifiers and the composed feature pieces. | Nothing in the app imports it — `main.tsx` is the entry. It exists so the component library has a boundary, and it is what `design-sync` bundles when publishing to Claude Design. `App` is deliberately absent: it is the shell, not a part. `ExternalLink` was missing from it until 017, having been added to `shared/ui` after the barrel was written. |
| Shop links | `src/domain/shopLinkRepository.ts` | `getShopLinks()` — bundled paint-id → retailer URL map. | No refresh and no cache, unlike the paint catalogue: a build-time crawl, read only by the export feature, and behind the `markdownExport` flag. Coverage is partial — see OPEN-ITEMS 3. |
| Test config | `vitest.config.ts` | Extends `vite.config.ts` through `mergeConfig` and adds only the `test` block. | Was a standalone config, and had already drifted: the app config's `@` → `src` alias was missing here, so the first module importing through `@/` would have typechecked, built, and failed to resolve only under test. Merging makes the alias and the React plugin shared by construction. |
| Upstream check | `.github/workflows/upstream-check.yml`, `tools/scraper/checkUpstream.mjs` | Runs the shared parser against the live documents weekly, and on any change to the parser, snapshot or scraper. | The alarm for the silent refresh: a format change upstream would otherwise strand every install on its bundled snapshot with no signal. Fails on fetch error, parser throw, a count under the rejection floor in total or per brand, or an unusable colour. A snapshot merely behind upstream is reported, not failed. Runs on Node 24 — it imports the parser as TypeScript directly. |
| Icon generator | `tools/icons/generate-icons.mjs` | One source mark becomes every launcher icon, both legacy shapes, the adaptive foreground, every splash canvas, the 512px Play icon and `public/favicon.svg`. | Refuses to run without `tools/icons/source/icon.{svg,png}` rather than emitting placeholder art. The adaptive foreground is transparent and inset to Android's 66dp safe zone; the legacy icon carries its own ground because pre-API-26 launchers have no layers to composite. Writes the ground colour as a resource so the XML cannot drift from the PNGs. |
| Store screenshots | `tools/store/screenshots.mjs` | Drives the built app in headless Chromium at 1080×1920 and captures the four listing screenshots. | Seeds `localStorage` directly instead of clicking two lists into being on every run. Blocks `githubusercontent.com` so a capture cannot depend on upstream or the network. Runs `vite preview` through node rather than `npx`, which on Windows is a `.cmd` node will not spawn without a shell. `launchBrowser` takes the first candidate that **starts**, not the first that exists — picking by `existsSync` chose an installed-but-broken Edge and reported "no Chromium found" while a working one sat on the same disk, which is what stranded the screenshots for two retros. Playwright's shared cache is a candidate; it is a convenience, not a dependency. |
| Feature graphic | `tools/store/feature-graphic.mjs` | Renders the 1024×500 Play listing banner. | Uses a browser rather than an image library so it can `@font-face` the app's own Cinzel woff2 — a compositing library would fall back to whatever serif the machine has. Assets are inlined as data URIs because `setContent` gives the page no origin to resolve relative URLs against. |
| Listing check | `tools/store/check-listing.mjs` | Checks every field in `store/listing.md` against the character limit declared in its own heading. | Play truncates some fields silently and rejects others. Reads the limits out of the document so new fields are covered without touching the script. |
| Search logic | `src/features/search/search.ts` | `searchPaints` — Fuse.js fuzzy search over the catalogue, optionally narrowed to one brand. | The Fuse index is rebuilt per call rather than memoised: the catalogue can be replaced underneath by the background refresh, and a stale index would keep answering with paints that have left it. `getAutocompleteSuggestions` and the exported `initializeSearchIndex` were removed in 017 — the first had no caller outside its own four tests, and the second was only ever internal. |
| Paint queries | `src/domain/paintQueries.ts` | Pure queries over the catalogue: `getUniqueBrands`, and `getTopMatches` which resolves a paint's stored equivalents against an id index. | Moved out of the deleted `features/browse` so views share them without importing across features. A `Match` is an id and a delta, so `getTopMatches` sorts, caps *and* resolves — dropping an id whose paint has left the catalogue rather than rendering a tile that jumps nowhere. The name-based `indexPaintsByName`/`paintNameKey` are gone: a name stopped identifying a paint once ranges arrived. |
| Color utilities | `src/shared/lib/color.ts` | The single source of truth for ΔE classification: `CLOSE_DELTA_MAX` (7), `VERY_CLOSE_DELTA_MAX` (3), `getDeltaQuality`, `getDeltaStyle`, `getDeltaLabel`. | Every view that renders a delta must classify through here — SearchSheet previously used its own 2/4 scale. |
| Design tokens | `src/shared/styles/tokens.css` | Every colour, radius, type size, tracking, elevation and structural spacing in the app. | The single source of truth — see [0.2-design-system.md](0.2-design-system.md). Extracted from 50 raw literals that had been hand-written across five modules and two components. Spacing is deliberately semantic rather than a numeric scale. |
| Global styles | `src/shared/styles/global.css` | Imports fonts and tokens; base reset, font/scroll defaults. | Held the `:root` token block until the design system was extracted; it now only consumes tokens. The dead `--bg-void` alias was dropped with it. |
| List palette | `src/shared/ui/listPalette.ts` | The five list banner colours and `DEFAULT_LIST_COLOR`. | Hex rather than CSS variables because the value is persisted: `var(--gold)` in localStorage is a string that means nothing outside a stylesheet. The picker and the store default were two copies of this list before it existed. |
| Background images | `public/imagery/bg-page.png`, `public/imagery/bg-card.png` | Dark texture (page bg) and dark swirling abstract art (card bg). | From the original Paint Codex design by [Lukas Stordeur](https://github.com/LukasStordeur) — see [0.2-design-system.md](0.2-design-system.md). |
| Tests | `src/test/` | Unit tests for store, search, export, lists panel, search sheet, catalogue parser, catalogue refresh, and the shipped snapshot itself. | 127 tests pass. `appVersion.test.ts` reads `build.gradle` and `project.pbxproj` as text and asserts every restatement of the release version against them — the only test here that touches the native projects. `ListsPanel.test.tsx` covers both flag states of the export action by supplying or omitting `onExportList`. `helpers/catalogMarkdown.ts` builds upstream-shaped tables so parser and refresh tests need no 270 KB fixture, and cases size them from the real snapshot's per-brand counts so a rescrape cannot turn the file red at once. `paintCatalogSync.test.ts` re-imports the module graph per case, since the catalogue is module state seeded at import. `paintSnapshot.test.ts` asserts against `paints.snapshot.json` itself, because `npm run scrape` is hand-run and never runs in CI. |

## Conventions

- **TypeScript is strict everywhere.** Every project (`app`, `test`, `node`, `tools`) sets `strict: true`. Test files are typechecked by `tsconfig.test.json`; they were excluded from every project until retro 005.
- **Delta classification goes through `shared/lib/color.ts`.** Never inline a threshold in a component.
- **A raw value may appear in a component stylesheet only if it is used exactly once.** The moment the same colour or number turns up in a second file it belongs in `shared/styles/tokens.css` under a name. This is written down because it was already broken: 50 raw colour literals across five modules, including `#dcb877` pasted three times while `--gold-title` sat defined and unused, and four greys hand-picked within 7 per channel of tokens that already existed. Full system in [0.2-design-system.md](0.2-design-system.md).
- **Chrome lives in `shared/ui/`, never in a feature.** A sheet, pill, badge, icon button, gold button, ghost button, text field or swatch comes from the primitive. A feature stylesheet is for what is genuinely specific to that screen — a result card, a section heading, a grid. The rule exists because it was broken: two overlays carried 43 byte-identical lines of sheet chrome, and the same button was written four ways.
- **Every outbound link goes through `EXTERNAL_LINK_PROPS`.** Never hand-type `target`/`rel` on an anchor. `target="_blank"` is load-bearing rather than cosmetic — it is the mechanism by which the Android WebView hands a URL to the system browser, and without it the link navigates the app away from itself with no way back. `AboutSheet.test.tsx` asserts the pair on *every* link it renders, not on a chosen one.
- **Overlays are a `Sheet`, not a hand-built dialog.** `Sheet` owns Escape, the focus trap and the dialog roles together, so the three cannot be wired one-and-a-half times. Only `useBackDismiss` stays with the caller, since the open flag does.
- **Cinzel labels, EB Garamond reads.** Every title, tab, chip and button is `--font-display` with tracking; body copy, inputs and prose empty states are `--font-body`. A control set in Garamond, or a sentence set in Cinzel, is a bug.
- **Never render `paint.matches` directly** — it stores ids and deltas, not paints. Go through `getTopMatches`, which sorts, caps and resolves, and which drops an id whose paint has left the catalogue instead of rendering a tile that jumps nowhere.
- **Overlays wire up all three**: `useBackDismiss` (Android), `useDismissOnEscape` (desktop) and `useFocusTrap`, and carry `role="dialog"`, `aria-modal` and `tabIndex={-1}`. The first two make it dismissable, the third keeps a keyboard user inside it.
- **Open work lives in `documentation/OPEN-ITEMS.md`**, not in per-retro deferred sections. A retro records what a session did; anything left undone goes on that one list and is deleted from it when closed.
- **One parser for the catalogue.** `parsePaintCatalog` in `domain/paintCatalogSource.ts` is the only code that reads upstream markup; `tools/scraper/scrape.mjs` and `checkUpstream.mjs` import it rather than carrying a copy. Two parsers would let a bundled snapshot and a refreshed one disagree about the same documents. Node loads that module through type stripping, so it must stay free of non-erasable syntax and of value imports from `src/` — which is why the CIELAB conversion sits inside it rather than in `shared/lib/color.ts`.
- **The catalogue is read through `getPaints()`, never imported.** `src/data/paints.snapshot.json` is the floor, not the source of truth — importing it directly pins a component to build-time data and it will miss the runtime refresh.
- **The refresh is silent.** No spinner, no toast, no error surface. Being offline is the normal case; the app is fully usable on the cached or bundled catalogue and simply shows better data if the fetch lands.
- **Lists persist paint ids, never Paint objects.** Resolve with `resolvePaints` at read time. Storing copies means a catalogue refresh never reaches saved lists. Any change to the persisted shape needs a `version` bump and a cumulative `migrate` branch — and so does a change to the *id scheme*, since `resolvePaints` drops what it cannot find and an unmigrated list empties itself with no message.
- **The catalogue refresh is the app's only network traffic.** It is three requests now rather than one, but to a single host and for a single purpose; anything user-visible about that count belongs in `store/privacy-policy.md`, which enumerates the URLs. Everything else — fonts, imagery, the snapshot it launches on — ships in the bundle, and the app is fully usable with the radio off. Adding a request for a *different* purpose needs a reason as good as this one's: the refresh exists because paint data goes stale between releases, and it degrades to "no change" rather than to a broken screen. A webfont or an analytics beacon does not clear that bar.
- **Tests run through the app's own Vite.** `vitest.config.ts` merges
  `vite.config.ts` rather than restating it, and Vitest is kept on a major that
  accepts the project's Vite as a peer instead of installing its own. Both rules
  exist because they were broken: the configs had drifted on the `@` alias, and
  Vitest 2 hard-depended on Vite 5 while the build used Vite 8 — so the suite
  was transforming and resolving code with a toolchain three majors from the one
  that ships. A green test run means less than it looks like when that is true.
- **Store assets are generated, never hand-exported.** Icons, splashes,
  screenshots and the feature graphic all come from `npm run` scripts over a
  single source. Hand-exporting is how a listing ends up showing last year's UI
  and an icon that is right in four density buckets out of five — the drift is
  invisible because nobody looks at their own listing.
- **Upstream is untrusted input.** It is a scraped third-party document, so `parsePaintCatalog` validates rather than assumes: colours are normalised to `#RRGGBB` or the entry is dropped, a row whose range is null is dropped, and `paintCatalogSync` rejects a parse yielding under half the bundled paint count in total or in any one brand. Loosening any of those puts unrenderable data straight into the UI.

## Paint catalogue

Upstream is `Arcturus5404/miniature-paints` (MIT), one markdown table per brand
served raw from GitHub. Paco reads three of them — `Citadel_Colour.md`,
`Vallejo.md`, `Army_Painter.md` — listed with their app-facing brand labels in
`PAINT_CATALOG_SOURCES`. They reach the app twice.

**At build time.** `npm run scrape` fetches all three, runs
`parsePaintCatalog`, and writes `src/data/paints.snapshot.json` minified. That
snapshot ships in the bundle and is what a freshly installed app shows.

**At launch.** `main.tsx` calls `refreshPaintCatalog()` once, at idle and
unawaited. It fetches the same three documents with `cache: 'no-cache'`, so the
launch really does check rather than trusting the five-minute `max-age`; an
unchanged upstream answers `304` with an empty body, which is why checking
every launch is affordable. All three or none: a partial fetch would parse
cleanly and replace a complete cache with one quietly missing a brand. If
anything changed, the set is parsed, sanity-checked, written to localStorage,
and published to subscribers — the UI re-renders with the new paints underneath
the user.

Reads stay synchronous throughout: the repository resolves cache-then-bundled
at import, so nothing ever waits on the network.

| Layer | Wins over | Where |
| --- | --- | --- |
| Refreshed this session | everything | memory, via `setPaints` |
| Cached from a past session | bundled | `localStorage['paco-paint-catalog']` |
| Bundled snapshot | — | `src/data/paints.snapshot.json` |

Four things keep a bad upstream from becoming a bad app: a parse yielding under
half the bundled paint count is rejected outright, so is one where any single
brand falls under half of what it shipped, a cache written by a build with a
different snapshot is discarded, and a cache that fails to validate is thrown
away rather than repaired. In every one of those cases the app falls back to
data it shipped with. The per-brand floor is not redundant with the total: the
realistic failure now is one file being renamed while the other two answer
normally, and Citadel is small enough that losing it whole still leaves the sum
well over half.

Cross-origin works because `raw.githubusercontent.com` answers
`Access-Control-Allow-Origin: *`, and the request sets no author headers, so it
takes no CORS preflight. Android needs `android.permission.INTERNET`, which the
manifest already declares.

### Paint ids

An id is brand + primary range + name — `citadel-base-abaddon-black`. The
range is in there because it has to be: upstream carries the same name in more
than one range at *different colours*, and Citadel's Administratum Grey really
is a different grey in Layer and in Air. The product code is deliberately not
in the id, even though it would be shorter and unique, because upstream leaves
it null for whole ranges and fills some in later — an id that changes when a
spreadsheet cell is filled in would silently empty every list referencing it.

Three things reduce the raw rows to entries:

1. **Rows identical in range, name and colour** are the same paint listed
   twice, and collapse.
2. **One colour under several range names** — Citadel's Abaddon Black as both
   Air and Base at `#000000` — merges into one entry whose `category` lists
   every range, primary first: `Base · Air`. On screen those rows were the same
   name, the same swatch and the same hex, so keeping them apart only ever
   produced duplicate search results and duplicate equivalent tiles. The ranges
   are all still recorded, so nothing is lost against upstream.
3. **What still clashes** — 17 rows sharing a name and primary range at
   different colours, Vallejo reissues under two product codes mostly — is
   ordered by code then colour and suffixed `-2`, `-3`. Ordering by the row's
   own content rather than its position keeps the suffixes from shuffling when
   upstream re-sorts.

"Primary range" is brush before airbrush, then alphabetical. Brush leads
because it is what someone browsing a colour usually wants, and because it is
what the pre-Arcturus catalogue contained, so carried-over lists land on it.

`src/data/paintIdMigration.json` carries saved lists across both id changes —
the move to range-qualified ids, and the range merge that followed. The store
applies the same map at v2 → v3 and v3 → v4; `buildIdMigration.mjs` refuses to
write a map containing an entry whose target is itself a key, which is what
makes applying it twice safe. Twenty legacy paints have no entry because the
new source does not carry them; those drop out at resolve time, as any retired
paint does. Mapping them to the nearest colour instead would have put a
different product in someone's list under the old name.

### Equivalents

Upstream ships colour, not conversions, so the equivalents are ours.
`attachMatches` converts every paint to CIELAB and keeps each one's six nearest
neighbours *from other brands* — the nearest Citadel paint answers a different
question than "what do I buy instead of this Citadel paint".

The metric is CIE76, plain Euclidean distance in CIELAB, because that is the
scale the thresholds in `shared/lib/color.ts` are already expressed on: 3 for
"indistinguishable", 7 for "still usable". CIEDE2000 models perception better
but returns systematically smaller numbers, so adopting it would silently
reclassify every match in the app without either constant changing.

Six is stored because six is what `SearchSheet` renders. The snapshot ships in
the bundle *and* is mirrored into localStorage, so every extra match is paid
for three times.

## Native

| Concern | Where | Notes |
| --- | --- | --- |
| Android back gesture | `@capacitor/app` + `src/shared/hooks/useBackDismiss.ts` | Capacitor 8 core registers no `onBackPressed`/`OnBackInvoked` handler, so the gesture reaches Android's default and finishes the activity. The plugin surfaces a `backButton` event the web layer can intercept. |
| Android toolchain | `android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.14.3 requires **JDK 21**. Android Studio's bundled JBR is Java 25 and fails with `Unsupported class file major version 69` once cached build scripts are invalidated. |
| Release signing | `android/app/build.gradle` | Upload key only — Google holds the app signing key under Play App Signing, so a lost upload key is recoverable. Credentials are read from the *user* `gradle.properties` and never enter the repo. A release build without them throws at configuration time; the failure mode it prevents is an unsigned bundle that builds, looks right, and is rejected on upload. |
| Launch appearance | `android/.../values/styles.xml` | The ground is declared three times because three different things paint it: `@drawable/splash` pre-API-31, `windowSplashScreenBackground` on API 31+ where the system draws its own splash and ignores the drawable, and `android:windowBackground` for the gap between the splash tearing down and the web layer's first paint. Miss any one and a near-black app flashes white on launch. |
